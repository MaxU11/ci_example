os: linux
  
language: go

install: travis_install_go_dependencies

go:
  - 1.13.x
  - master
  
dist: trusty
sudo: false

script:
  - env GO111MODULE=on go build
  - env GO111MODULE=on go test
  - go test -v ./...
  - CGO_ENABLED=0 go build -o demo
  
